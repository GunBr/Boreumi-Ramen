# 보름이의 라면포차 V0.21 패치노트

## 핵심 업데이트: 아이폰 설치형 PWA

V0.20 게임을 아이폰 홈 화면에서 앱처럼 실행할 수 있도록 PWA 구조로 전환했습니다.

### 홈 화면 앱

- 앱 이름, 시작 주소, 색상, 가로 방향과 아이콘을 정의한 `app.webmanifest`를 추가했습니다.
- 보름달 간판을 180/192/512px 홈 화면 아이콘으로 사용합니다.
- 아이폰용 전체화면 메타데이터와 검은 반투명 상태 표시줄 설정을 추가했습니다.
- 홈 화면 아이콘으로 실행하면 일반 Safari 주소창 없이 게임 화면이 열립니다.

### 가로 화면 전용 실행

- PWA 기본 방향을 `landscape`로 지정했습니다.
- 지원되는 모바일 환경에서는 실행 시 `screen.orientation.lock("landscape")`도 요청합니다.
- 기존의 `가로 모드로 돌려주세요` 전체 화면 안내를 삭제했습니다.
- 방향 잠금 API를 지원하지 않거나 iOS가 요청을 거부해도 게임을 가리는 안내 화면은 나타나지 않습니다.

### 아이폰 화면 대응

- `viewport-fit=cover`를 유지하고 노치·다이내믹 아일랜드의 안전영역을 적용했습니다.
- 설치형 전체화면과 일반 브라우저 실행 모두에서 내부 무대가 화면을 벗어나지 않도록 했습니다.
- 확대, 스크롤, 길게 누르기 선택과 오버스크롤을 차단해 게임 조작에 맞췄습니다.

### 오프라인 실행

- 서비스 워커를 추가해 HTML, CSS, JavaScript와 게임 일러스트를 저장합니다.
- 첫 온라인 실행 후 서버가 끊겨도 설치형 앱을 다시 열 수 있습니다.
- 새 V0.21 캐시가 활성화되면 이전 라면포차 캐시를 자동 정리합니다.
- 페이지 이동은 네트워크를 우선 사용하고 연결이 없을 때 저장된 게임 화면으로 대체합니다.

### 저장 데이터

- 저장 키를 V0.21 형식으로 갱신했습니다.
- V0.20의 DAY, 골드, 포차·조리도구 레벨, 손님·이야기·포장 기록을 자동 이전합니다.

## 검증

- 브라우저 자동검사 150개 전체 통과
- 아이폰 가로 CSS 화면 852×393에서 무대 전체가 화면 안에 들어오는 것 확인
- 실제 포인터로 `영업 시작 → 면 드래그 → 냄비 조리 시작` 확인
- 회전 안내 화면이 생성되지 않는 것 확인
- 서비스 워커 등록과 전체 게임 에셋 캐시 목록 확인
- 로컬 서버를 완전히 끈 뒤 페이지를 다시 열어 오프라인 실행 확인
- 브라우저 경고와 실행 오류 없음
