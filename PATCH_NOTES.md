# 보름이의 라면포차 V0.12 패치노트

## 핵심 변경

- 모바일 가로 전용 `1600×780` 무대와 V0.11.1의 실제 화면 배율·포인터 좌표 방식을 유지했습니다.
- 승인 레이아웃에 맞춰 상단 손님 3명, 왼쪽 냄비 3개, 오른쪽 그릴 2개와 오뎅바 1개, 하단 재료창을 배치했습니다.
- 면·계란·군만두·오뎅을 알맞은 조리기구에 드래그할 수 있습니다.
- 각 조리기구는 독립적으로 즉시 조리를 시작하고 동시에 진행됩니다.
- 드롭과 동시에 보름이가 해당 위치로 순간이동해 짧은 조리 동작을 보여줍니다. 순간이동 연출은 판정과 타이머를 지연시키지 않습니다.
- 완성 음식은 세 손님의 주문 말풍선 어디로든 드래그해 판매할 수 있습니다.
- 보름이와 뽀미는 V0.11.1의 승인 외형을 기준으로 제작한 정식 포즈 프레임을 사용합니다.
- 배경 광원, 카운터 그림자, 조리기구 깊이, 캐릭터 접지 그림자와 레이어를 다듬었습니다.
- 화면을 `손님 3명+뽀미 → 보름이 전용 가로 이동선 → 냄비 3개+그릴 2개+오뎅바 → 재료창`의 네 줄 구조로 다시 배치했습니다.
- 보름이는 전용 이동선에서 드롭한 조리기구의 가로 위치로 즉시 이동하고, 다음 조작 전까지 그 자리에 머뭅니다.
- 뽀미는 손님 줄 오른쪽 고정 위치에서 이동하지 않고 잠자기·그루밍·손 흔들기 포즈만 순환합니다.

## V0.12 아트 시스템 완성

- 회사원·배달기사·학생을 각각 독립된 정식 SD 손님 일러스트로 교체했습니다.
- 냄비·그릴·오뎅바 및 라면·군만두·오뎅을 동일한 원근과 조명의 정식 일러스트로 교체했습니다.
- 라면·군만두·오뎅은 `빈 조리기구 → 조리 중 → 완성`의 단계마다 서로 다른 그림으로 변화합니다.
- 보름이는 면 넣기·계란 깨기·그릴 뒤집기·오뎅 건지기 네 가지 실제 포즈로 드롭 동작을 표현합니다.
- 뽀미는 CSS 변형이 아닌 잠자기·그루밍·손 흔들기 세 장의 실제 포즈 프레임을 사용합니다.
- 캐릭터 접지 그림자, 조리기구 준비광, 음식 완성광과 전체 레이어 깊이를 통일했습니다.

## V0.12 플레이 피드백 수정

- 완성 라면을 양쪽 손잡이와 오른쪽 조절부까지 온전히 보이는 고해상도 단독 일러스트로 교체했습니다.
- 완성 군만두가 접시로 바뀌지 않고 같은 그릴 위에서 자연스럽게 노릇해지도록 교체했습니다.
- 보름이 조리 포즈에서 냄비·버너·그릴·오뎅바를 제거하고 손과 작은 도구만 남겨 실제 조리기구가 계속 보이도록 했습니다.
- 영업 전과 손님 퇴장 후에는 빈 의자가 보이고, 입장한 자리만 손님·주문·대기 게이지가 나타납니다.
- 재료와 토핑은 텍스트나 이모지가 아닌 재료 일러스트로 드래그됩니다.
- 완성 음식은 조리기구에 표시된 것과 동일한 일러스트로 손님에게 드래그됩니다.

## 시스템 구조

- `Config`: 해상도, 조리시간, 가격 설정
- `State`: 영업·시간·매출·드래그 상태
- `Appliances`: 냄비·그릴·오뎅바의 독립 상태와 타이머
- 조리, 손님/판매, 입력/드래그, 캐릭터 연출, 화면 배율 로직을 분리했습니다.

## 조작

1. `영업 시작`을 누릅니다.
2. 재료를 알맞은 조리기구로 드래그합니다.
3. 완성 표시가 난 음식을 손님 말풍선으로 드래그합니다.

## 브라우저 검증

- Chrome 실제 실행 화면에서 영업 시작 버튼과 빈 의자 → 손님 입장 → 주문 → 조리 → 완성 → 서빙 → 퇴장 흐름을 확인했습니다.
- 냄비 3개, 그릴 2개, 오뎅바 1개가 보름이 조리 동작 중에도 계속 유지되는 것을 확인했습니다.
- 재료 드래그와 완성 음식 드래그 모두 텍스트·이모지 대신 해당 일러스트가 표시되는 것을 확인했습니다.
- `1600×780` 기준 화면과 `844×390` 모바일 가로 화면에서 겹침·잘림·하단 재료창 충돌이 없음을 확인했습니다.

## V0.12 조리·서빙 연출 보강

- 빈 오뎅바 일러스트를 슬롯 안쪽으로 축소 배치해 오른쪽 조절부까지 여백과 함께 보이도록 수정했습니다.
- 면만 조리한 라면은 계란 없는 완성 라면으로, 조리 중 계란을 추가한 라면은 계란 라면으로 서로 다른 완성 일러스트가 표시됩니다.
- 완성 음식은 주문 말풍선뿐 아니라 해당 손님 캐릭터와 손님 자리 전체에 드롭해도 서빙됩니다.
- 보름이는 평상시 중앙에서 앞치마의 `보름이의 라면포차` 문구가 보이는 자연스러운 정면 전신 대기 포즈를 사용합니다.
- 면 넣기·계란 깨기·그릴 뒤집기·오뎅 건지기는 조리기구 바로 뒤에서 확대된 전용 상반신 포즈로 표시되며 손과 도구가 조리기구 위까지 이어집니다.
- 서빙할 때 보름이가 손님 앞으로 순간이동해 트레이를 든 전신 뒷모습을 잠시 보여준 뒤 중앙 대기 위치로 돌아옵니다.
- 서빙 뒷모습의 크기를 키우고 발 위치를 조리 통로 바닥선까지 내려, 손님을 일부 가리더라도 테이블 위가 아닌 카운터 앞 바닥에서 음식을 건네도록 수정했습니다.
- 포인터 이동과 놓기 판정을 화면 전체에서 받도록 보강해 모바일 터치와 브라우저 마우스 드래그의 안정성을 높였습니다.
- 실제 브라우저에서 완성 라면을 주문 말풍선이 아닌 손님 캐릭터 몸 위로 드래그해 매출·퇴장·서빙 포즈가 정상 처리되는 것을 확인했습니다.

## V0.12 포장마차 환경 완성

- 단색에 가깝던 후면을 한강변을 떠올리는 깊은 밤의 강·도시 불빛·물결이 이어지는 전용 야경 일러스트로 교체했습니다.
- 간판 옆에 따뜻한 보름달을 배치하고 달 표면의 은은한 무늬, 주변 광원, 강 위로 끊어지는 반사광을 별도 레이어로 구현했습니다.
- 차양 아래 전등을 작은 포차 등불 형태로 정리하고, 손님 테이블과 조리 통로에 따뜻한 주황빛이 번지도록 조명을 보강했습니다.
- 손님 앞쪽에 두께가 있는 원목 테이블을 추가해 손님과 보름이의 앞뒤 관계가 자연스럽게 보이도록 했습니다.
- 보름이가 서 있는 공간은 원근감이 있는 나무 바닥과 그림자, 바닥 광원으로 구성해 빈 갈색 벽처럼 보이던 문제를 수정했습니다.
- 테이블 양쪽에는 휴지·수저·양념통을 묶은 작은 포차 소품을 추가했으며, 손님·뽀미·주문 표시를 가리지 않도록 가장자리와 빈 간격에 배치했습니다.
- 조리대에는 나뭇결, 상판 하이라이트, 하단 음영을 더해 냄비·그릴·오뎅바가 배경에 붙어 보이지 않도록 깊이감을 높였습니다.
- 실제 브라우저에서 영업 전 빈 의자 화면과 영업 중 손님 화면을 확인했고, 전체 자동 회귀 검증 결과 `PASS`를 확인했습니다.
- 실제 `844×390` iframe 뷰포트에서 모바일 가로 화면을 재현해 달·손님·보름이·조리기구 6개·하단 재료창의 겹침과 잘림이 없음을 확인했습니다.

## V0.12 1080p·확장 플레이 존

- 기존 `1600×780` 고정 무대를 중앙 `1920×1080` 안전영역으로 교체했습니다.
- 화면비가 넓은 기기에서는 무대가 최대 `2340×1080`까지 자동 확장되며, 현재 게임 UI는 중앙 1920 영역 안에서 같은 비율과 간격을 유지합니다.
- iPhone 16e·iPhone 17과 비슷한 `844×390` 화면비에서는 내부 무대가 실제 `2337×1080`으로 계산되어 좌우에 각각 약 208px의 추가 공간이 열립니다.
- 손님 3명, 뽀미, 보름이 이동 통로, 냄비 3개, 그릴 2개, 오뎅바, 하단 재료창을 1080p 좌표계에 맞춰 크기와 간격을 다시 조율했습니다.
- 보름이의 대기·조리·서빙 폭과 순간이동 경계를 현재 무대 폭에서 계산하도록 변경해, 확장 화면에서도 잘리거나 중앙으로 쏠리지 않습니다.
- 넓어진 좌우 영역은 현재 장식 공간으로만 표시되며, 이후 레벨에서 손님석을 5자리까지 늘리고 `포장 주문`과 `완성대`를 추가할 수 있도록 설정에 예약했습니다.
- 하단 터치 영역은 1080p 기준으로 확대했으며, 아이폰형 화면에서 축소된 뒤에도 재료와 조리기구의 실제 터치 크기가 충분히 유지됩니다.
- 실제 브라우저에서 `1920×1080` 안전 무대와 `2337×1080` 확장 무대 모두 자동 주요 흐름 검증 `PASS`를 확인했습니다.
- 두 화면비 모두 실제 포인터 드래그로 면을 냄비에 넣어 조리 시작, 보름이 순간이동, 조리기구 유지가 정상 동작하는 것을 확인했습니다.

## V0.12 배경·달·캐릭터 축척 재조정

- 별도 CSS 원으로 표시되던 보름달을 제거하고, 달·도시·강물·반사광이 하나의 광원과 원근으로 이어지는 새 강변 야경 일러스트로 교체했습니다.
- 새 야경 원본을 `3344×1882`로 준비해 1920×1080 및 확장 화면에서 배경이 지나치게 흐려지지 않도록 했습니다.
- 도시선은 손님석 뒤의 열린 부분에서 보이도록 높이고, 달은 차양과 테이블 사이의 하늘에 자연스럽게 나타나도록 구도를 조정했습니다.
- 포차 등불 위치를 네 곳으로 다시 나눠 달 위에 주황색 불빛이 겹치던 문제를 수정했습니다.
- 서비스 통로의 한쪽으로 쏠린 삼각형 조명을 부드러운 양쪽 확산광으로 바꿔 바닥에 인위적인 스포트라이트가 생기지 않게 했습니다.
- 보름이 대기 포즈를 `330×450`, 조리 포즈를 `450×430`, 서빙 포즈를 `410×540` 영역으로 확대해 테이블·손님·조리도구와 인물 축척이 맞도록 했습니다.
- 평상시 보름이를 화면 정중앙에서 조금 왼쪽의 빈 공간으로 옮겨 중앙 빈 의자가 머리 뒤에 겹치지 않게 했습니다.
- 1920×1080과 아이폰형 2337×1080 무대 모두 자동 주요 흐름 검증 `PASS`를 확인했고, 아이폰형 화면에서 실제 면 드래그도 다시 확인했습니다.

## V0.12 레벨 1 포차 콘셉트 재구성

- 초안 콘셉트의 핵심인 `작지만 따뜻하고 북적이는 포장마차`를 기준으로 전체 화면 밀도를 다시 구성했습니다.
- 별도의 넓은 보름이 이동 통로를 화면에서 제거하고 손님 카운터, 보름이, 뽀미, 조리대와 음식이 한 장면 안에서 가깝게 이어지도록 했습니다.
- 목재 벽과 기둥, 전구, 식기·술병·양념통·메모·별 장식, 작은 야경 창문으로 구성된 레벨 1 전용 포차 내부 배경을 새로 제작했습니다.
- 새 포차 내부 배경은 `3344×1882` 크기로 준비해 1920×1080과 아이폰형 확장 화면에서 선명도를 유지합니다.
- 손님 캐릭터와 주문 말풍선을 키우고 손님 바로 앞에 원목 카운터를 배치해 주문·표정·대기 상태가 더 가깝게 보이도록 했습니다.
- 냄비 3개, 그릴 2개, 오뎅바 1개를 전경으로 올리고 높이를 키워 냄비 속 음식과 완성 음식이 화면의 중심이 되도록 했습니다.
- 보름이는 평상시 조리대 중앙 가까이에 서고, 재료를 놓으면 별도 거대 상반신 사진 대신 기존 전신 조리 포즈가 해당 조리기구 뒤로 순간이동합니다.
- 전신 조리 포즈의 하체는 연속된 조리대와 조리기구가 자연스럽게 가려 초안처럼 허리 위의 손과 조리 도구가 음식에 연결되어 보입니다.
- 조리 중·완성 상태에는 조리기구 위로 은은한 김이 올라오며 음식 일러스트를 더 크게 표시합니다.
- 뽀미는 보름이 옆 전경 카운터의 고정 위치에서 잠자기·그루밍·손 흔들기 포즈만 바뀝니다.
- 아이폰형 넓은 화면에서는 포차 배경과 카운터가 좌우로 이어지고, 미래 손님석을 위한 빈 이동장을 현재 레벨에 미리 노출하지 않습니다.
- 내부 설정에는 레벨 1 손님 3명과 향후 최대 5명, 포장 주문대와 완성대 예약을 그대로 유지해 다음 레벨에서 포차 자체가 확장될 수 있습니다.
- 1920×1080과 아이폰형 2337×1080 화면 모두 자동 주요 흐름 검증 `PASS`를 확인했고, 두 화면비에서 실제 면 드래그와 전신 조리 포즈 연결을 확인했습니다.

## V0.12 빈자리 의자 개선
- 손님이 없는 자리에서는 의자의 좌판과 다리가 보이지 않고, 손님용 카운터 뒤로 나무 등받이만 살짝 보이도록 수정했습니다.
- 손님이 등장하면 등받이 투명도를 낮춰 캐릭터 뒤에서 거의 드러나지 않도록 조정했습니다.
- 실제 브라우저에서 빈자리 3개와 손님 등장 상태를 각각 확인했습니다.
