# 보름이의 라면포차 V0.22 패치노트

## 모바일 가로 전체 화면

- PWA 매니페스트의 가로 방향과 지원 환경의 방향 잠금 요청을 유지했습니다.
- 모바일이 세로 크기로 시작해도 게임 전체를 90도 회전해 기기 화면 100%를 사용합니다.
- 회전된 화면의 터치 좌표를 가로 게임 좌표로 변환해 재료·음식 드래그가 어긋나지 않게 했습니다.
- 기존처럼 방향 전환 안내로 게임을 가리는 화면은 사용하지 않습니다.
- 노치와 다이내믹 아일랜드를 위한 안전영역 처리를 유지합니다.

## 독립 연습 포차 튜토리얼

- 실제 영업 전에 따로 실행되는 연습 포차를 추가했습니다.
- 연습 중에는 DAY 시간과 손님 인내심이 정지합니다.
- 매출, 서빙 수, 손님 기록, 성장 데이터는 변경하거나 저장하지 않습니다.
- 첫 주문은 안내와 정확히 맞는 `기본 라면 + 소주`로 고정했습니다.
- `주문 확인 → 면 조리 → 완성 대기 → 라면 서빙 → 소주 서빙 → 영업 화면` 흐름으로 구성했습니다.
- 완료 후 보름이가 즉시 중앙 대기 위치로 돌아오고 빈 영업 화면에서 시작합니다.

## 시작 로딩 화면

- 게임 데이터, 글꼴, PWA 준비 상태와 핵심 이미지 24개를 확인하는 로딩 단계를 추가했습니다.
- 진행 막대, 현재 준비 내용과 백분율을 표시합니다.
- 준비가 끝나기 전에는 게임 화면과 튜토리얼 조작이 시작되지 않습니다.
- 로딩 완료 후 자연스럽게 게임 또는 첫 실행 튜토리얼로 전환합니다.

## 저장과 오프라인

- 저장 키를 V0.22 형식으로 갱신했습니다.
- V0.21의 DAY, 골드, 포차·조리도구 레벨, 단골·이야기·포장 기록을 자동 이전합니다.
- 서비스 워커 캐시를 V0.22로 갱신하고 새 캐시가 활성화되면 이전 버전 캐시를 정리합니다.

## 검증

- 브라우저 자동검사 155개 전체 통과
- 아이폰 가로 비율 852×393에서 로딩·튜토리얼·영업 화면 전체 표시 확인
- 세로 시작 비율 393×852에서 회전된 게임이 화면 전체를 채우는 것 확인
- 가로 화면과 회전 화면 모두 실제 터치로 `면 → 냄비` 드래그 성공
- 실제 터치로 `라면 → 손님`, `소주 → 손님` 서빙과 튜토리얼 완료 확인
- 튜토리얼 종료 후 시간·매출·저장 기록이 남지 않고 실제 영업 화면으로 복귀하는 것 확인
- JavaScript 문법, PWA 구성 파일, 서비스 워커 에셋 목록 확인
