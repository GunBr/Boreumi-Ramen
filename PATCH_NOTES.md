# 보름이의 라면포차 V0.23 패치노트

## 모바일 실기기 안정화

- 아이폰의 Visual Viewport 변화를 기준으로 가로 무대를 다시 계산합니다.
- 세로 크기로 시작한 회전 화면에서도 노치 안전영역과 드래그 좌표가 어긋나지 않게 보강했습니다.
- 도움말과 소리 버튼은 그림 크기를 유지하면서 약 44px의 터치 판정을 갖습니다.
- 모바일 관리 버튼도 약 44×44px 이상으로 조정했습니다.
- 확대 제스처, 이중 화면 이동과 오버스크롤로 조작이 끊기는 현상을 방지했습니다.

## 로딩·리소스 최적화

- 핵심 일러스트 24개를 순차 방식에서 4개 병렬 로딩으로 변경했습니다.
- 승인된 일러스트 64개를 픽셀 손실 없는 WebP로 전환했습니다.
- 실행 일러스트에서 약 12.02MiB를 절약했습니다.
- 전체 게임 폴더 용량이 약 52.57MiB에서 40.56MiB로 22.8% 감소했습니다.

## 업데이트와 오프라인

- 오프라인 리소스 저장 개수와 완료 상태를 도움말에서 확인할 수 있습니다.
- 새 서비스 워커가 준비되면 상단에 업데이트 알림이 나타납니다.
- `지금 적용`을 누르면 새 캐시로 전환한 뒤 게임이 자동으로 다시 열립니다.
- 느린 연결은 일정 시간 후 저장된 게임 화면으로 전환해 오프라인 시작 대기를 줄였습니다.
- 업데이트 확인 버튼을 추가했습니다.

## 저장 백업과 복구

- V0.23 전용 저장 키와 자동 백업 키를 추가했습니다.
- 새 저장 전에 이전의 정상 저장 기록을 자동 백업합니다.
- 기본 저장이 손상되면 자동 백업을 읽어 복구합니다.
- 현재 DAY와 성장 기록을 JSON 파일로 내보내고 다시 불러올 수 있습니다.
- V0.22 저장·소리·튜토리얼 완료 기록을 자동 이전합니다.

## 튜토리얼 연결

- 건너뛰기와 다시 보기 기록을 V0.23으로 이전했습니다.
- 연습 포차를 끝내면 빈 영업 화면으로 돌아오고 `영업 시작` 버튼을 강조합니다.
- 실제 첫 영업을 누르는 순간 강조가 사라지고 정상 DAY가 시작됩니다.

## 검증

- 브라우저 자동검사 161개 전체 통과
- 852×393 아이폰 가로 비율에서 로딩·튜토리얼·데이터 관리 화면 확인
- 932×430 긴 가로 비율에서 좌석 10개·조리도구 6개·포장 주문·완성대 무겹침 확인
- 393×852 세로 시작 회전 화면에서 전체 무대 표시와 실제 면 드래그 확인
- 실제 라면·소주 서빙과 튜토리얼 종료 후 첫 영업 연결 확인
- 업데이트 발견 → 지금 적용 → 자동 재실행 흐름 확인
- 서버 종료 후 오프라인 재실행 확인
- 무손실 WebP 64개와 런타임 참조·서비스 워커 캐시 누락 0개 확인
