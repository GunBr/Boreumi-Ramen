# 보름이의 라면포차 V0.26 패치노트

## 메뉴·레시피 시스템

- 메뉴·레시피 수첩을 추가했습니다. 라면·안주·주류의 그림, 판매가, 재료 조합, 원가, 기본 이익과 해금 조건을 한 화면에서 확인할 수 있습니다.
- 계란 대파 라면, 김치 계란 라면, 치즈 계란 라면, 김치 치즈 라면 4종을 추가했습니다.
- 승인된 조합만 만들 수 있고 토핑은 최대 두 종류까지 넣을 수 있습니다. 수첩에 없는 조합은 재료가 차감되기 전에 안내합니다.
- 복합 라면은 조리 중 두 토핑이 각각 보이며, 완성·드래그·손님 주문·포장 주문에서 같은 전용 일러스트를 사용합니다.
- 포차 레벨 상승으로 메뉴가 열리면 새 메뉴 그림과 조합을 알리는 해금 알림이 차례로 표시됩니다.

## 재료 상점과 재고

- 정산 화면의 재료 상점에 개별 구매 화면을 추가했습니다.
- 재료마다 현재/최대 재고, 1개 가격, 해금 레벨을 보여줍니다.
- `+1`, `+5`, `가득`, `열린 재료 전체 보충`을 지원합니다.
- 보유 골드가 부족하거나 재고가 가득 찬 구매 버튼은 자동으로 비활성화됩니다.

## 사용성 보완

- 튜토리얼 마지막에 하단 재고 숫자와 영업 후 구매 방법을 설명하는 7번째 단계를 추가했습니다.
- 주문·포장 주문의 음식 그림을 조금 더 크고 선명하게 표시합니다.
- 재료 진열대 페이지 표시를 배지 형태로 강화했습니다.
- 개발 도구(`?dev`)의 저장은 일반 플레이와 계속 분리됩니다.

## 저장 호환

- V0.25 저장 기록은 V0.26 첫 실행 때 자동으로 이어받습니다.
- 이전 저장에 없던 메뉴 해금 기록은 현재 포차 레벨을 기준으로 안전하게 채웁니다.

## 새 아트 제작

- 기존 완성 라면 그림을 편집 대상으로 삼아 복합 라면 4종을 built-in 이미지 생성 도구로 제작했습니다.
- 최종 사용 파일은 `assets/art-v026/*-v1.webp`이며, 크로마키 제거 후 투명 배경으로 검증했습니다.
- 제작 지시의 공통 기준은 기존 냄비·화구·시점과 따뜻한 게임 화풍을 그대로 유지하고, 요청한 두 토핑만 자연스럽게 추가하며 글자 없이 녹색 크로마키 배경으로 출력하는 것이었습니다.

## 최종 검증

- 실제 Chrome 모바일 가로 화면 `844×390` 자동 검사 174개를 통과했습니다.
- `1920×1080`에서 해금 알림, 메뉴 수첩, 조리, 정산, 개별 구매와 금액 반영을 직접 확인했습니다.
- 정산 화면 위에서 개별 재료 상점이 터치되지 않던 겹침 문제를 발견해 상점 표시 순서를 수정했습니다.
