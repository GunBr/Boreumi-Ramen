# 보름이의 라면포차 V0.26.3 작업 인계

## 현재 기준

- 버전: V0.26.3
- 일반 플레이: `index.html`
- 개발 도구: `index.html?dev`
- 자동 검사: `index.html?qa&silent`
- 일반 플레이와 개발 도구는 같은 게임 코드와 화면을 사용하고 개발 도구만 추가로 표시됩니다.

## 이번 버전의 확정 구성

- 목표 금액과 목표 달성 보너스는 사용하지 않습니다.
- 상단 두 번째 금액 칸과 영업일지 첫 칸은 총 누적 매출입니다.
- 사용자 화폐 표현은 `골드`가 아니라 `원`과 `보유 금액`입니다.
- 대화는 손님 아래에서 반투명 상태로 유지되며, 마우스를 올리면 불투명해지고 `○` 버튼으로 닫습니다.
- 조리도구 아래에는 새 원목 조리대가 있습니다.
- 오뎅바는 한 번 완성하면 계속 서빙할 수 있으며 그림의 꼬치 수만 3→2→1로 줄어듭니다.

## 주요 변경 파일

- `index.html`: V0.26.3 화면 구성과 표기
- `script.js`: 누적 매출, 대화, 오뎅 연속 서빙, 정산, 자동 검사
- `patch-v0263.css`: PC 버튼, 대화, 영업일지, 배경, 새 그림 배치
- `service-worker.js`: V0.26.3 오프라인 캐시
- `boot-v024.js`, `pwa-v024.js`: 버전 및 시작 처리
- `assets/art-v0263/`: 새 조리대와 오뎅바 2개·1개 그림

## 다음 작업 전 확인

- 변경은 항상 새 버전 폴더를 복사해 진행합니다.
- 빠른 기능 검사는 `?dev`, 전체 회귀 검사는 `?qa&silent`를 사용합니다.
- 서비스 워커 때문에 이전 파일이 보이면 캐시 버전과 `index.html`의 파일 쿼리 번호를 함께 올립니다.
- GitHub 저장소에는 100MB를 넘는 버전 ZIP을 커밋하지 말고, 압축을 푼 프로젝트 파일만 올립니다.

