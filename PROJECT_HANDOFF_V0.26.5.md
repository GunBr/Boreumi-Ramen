# 보름이의 라면포차 V0.26.5 작업 인계

## 현재 기준

- 버전: V0.26.5
- 일반 플레이: `index.html`
- 개발 도구: `index.html?dev`
- 자동 검사: `index.html?qa&silent`
- 일반 플레이와 개발 도구는 같은 게임 코드와 화면을 사용합니다.

## 이번 버전의 확정 구성

- 손님 줄은 무대 기준 `top:172px`, 수납형 테이블은 `top:442px`, `height:190px`입니다.
- 테이블 아래선은 배경 선반선 `y=632`에 맞고 너비는 무대 전체 `100%`입니다.
- 뽀미는 오른쪽에서 `top:323px` 위치로 새 상판 위에 배치됩니다.
- 테이블 앞면 수납칸에는 소스통·꼬치·젓가락·수저·그릇·술병이 들어갑니다.
- 정산 화면은 좌측 `38%`, 우측 `62%`의 제한된 영역을 사용하며 모든 하위 요소가 부모 칸을 넘지 않습니다.
- 다음 날 영업 시작 전에 `resetDailyOden()`을 호출해 오뎅바를 빈 상태로 되돌립니다.

## 주요 변경 파일

- `patch-v0265.css`: 손님 줄·뽀미·전폭 테이블 위치와 겹침 방지 정산 그리드
- `script.js`: 다음 날 오뎅 초기화와 V0.26.5 자동 검사
- `assets/art-v0265/`: 새 수납형 손님 테이블 원본·투명 PNG·WebP
- `boot-v024.js`, `pwa-v024.js`, `service-worker.js`: V0.26.5 리소스와 캐시

## 다음 작업 전 확인

- 변경은 항상 새 버전 폴더를 복사해 진행합니다.
- 빠른 기능 검사는 `?dev`, 전체 회귀 검사는 `?qa&silent`를 사용합니다.
- 서비스 워커 때문에 이전 화면이 보이면 캐시 버전과 `index.html`의 파일 쿼리 번호를 함께 올립니다.
- GitHub 저장소에는 100MB를 넘는 버전 ZIP을 커밋하지 말고, 압축을 푼 프로젝트 파일만 올립니다.

