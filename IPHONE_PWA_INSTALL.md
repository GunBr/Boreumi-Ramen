# 아이폰 PWA 배포·설치 안내

## 1. GitHub Desktop으로 V0.23 올리기

V0.23 ZIP을 압축 해제한 뒤 `Boreumi-Ramen_V0.23` 폴더 안의 내용물을 기존 GitHub 저장소의 게임 위치에 넣습니다. ZIP 파일 자체는 저장소에 넣지 않습니다.

`index.html`, `app.webmanifest`, `service-worker.js`, `assets` 폴더가 같은 배포 위치에 있어야 합니다.

1. GitHub Desktop에서 저장소를 엽니다.
2. `Show in Explorer`로 저장소 폴더를 엽니다.
3. 기존 게임 파일을 V0.23 내용물로 교체합니다.
4. GitHub Desktop의 Summary에 `V0.23 모바일 안정화`를 입력합니다.
5. `Commit to main`을 누릅니다.
6. `Push origin`을 누릅니다.

## 2. GitHub Pages 확인

Push가 끝난 뒤 기존 Pages 주소가 갱신될 때까지 잠시 기다립니다. 처음 설정한다면 GitHub 웹사이트의 `Settings → Pages`에서 `Deploy from a branch`, `main`과 게임 파일 위치를 선택합니다.

PWA 설치와 오프라인 기능에는 HTTPS 주소가 필요합니다. ZIP 안의 `index.html`을 아이폰에서 직접 여는 방식으로는 설치형 기능이 작동하지 않습니다.

## 3. 아이폰 홈 화면에 설치하기

1. 아이폰 Safari에서 GitHub Pages 주소를 엽니다.
2. 공유 버튼을 누릅니다.
3. `홈 화면에 추가`를 선택합니다.
4. 이름이 `라면포차`인지 확인하고 `추가`를 누릅니다.
5. 홈 화면의 보름달 간판 아이콘으로 실행합니다.

처음 한 번은 인터넷에 연결한 상태로 오프라인 저장이 완료될 때까지 기다려 주세요. 게임의 `?` 버튼에서 저장 진행 상황을 확인할 수 있습니다.

## 4. 새 버전 적용

새 버전을 GitHub에 Push하면 게임이 새 파일을 준비합니다. 준비가 끝나면 화면 위쪽의 `새 버전 준비가 끝났어요` 알림에서 `지금 적용`을 누릅니다. 게임이 자동으로 다시 열리면 업데이트가 완료된 것입니다.

알림이 보이지 않으면 `? → 업데이트 확인`을 누르세요.

## 5. 저장 기록 백업

`? → 저장 백업`을 누르면 현재 진행 상황이 JSON 파일로 저장됩니다. 복원이 필요할 때 영업을 마친 상태에서 `백업 불러오기`를 누르고 해당 파일을 선택합니다.

V0.23은 기기 안에도 직전 정상 저장을 자동 백업하므로 기본 저장이 손상되면 자동으로 복구합니다.

## 가로 방향

설치형 앱은 가로 방향을 우선 요청합니다. iOS가 방향 잠금을 허용하지 않아 세로 크기로 시작해도 게임 전체를 회전해 화면을 채우므로 휴대폰을 가로로 잡으면 전체 게임이 보입니다. iOS 회전 잠금 설정까지 무시하는 운영체제 수준 고정은 이후 네이티브 앱 포장에서 적용할 수 있습니다.
