# 아이폰 PWA 배포·설치 안내

## 1. GitHub에 올리기

V0.22 ZIP을 압축 해제한 뒤 `Boreumi-Ramen_V0.22` 폴더 안의 내용물을 기존 GitHub 저장소의 게임 위치에 덮어씁니다. `index.html`, `app.webmanifest`, `service-worker.js`, `assets` 폴더가 같은 배포 위치에 있어야 합니다.

GitHub Desktop에서 변경 내용을 확인하고 요약에 `V0.22 모바일 시작·튜토리얼·로딩`처럼 적은 뒤 `Commit to main`과 `Push origin`을 차례로 누릅니다.

## 2. GitHub Pages 확인

기존 Pages 주소를 사용하고 있다면 Push가 끝난 뒤 배포가 갱신될 때까지 잠시 기다립니다. 처음 설정한다면 GitHub 웹사이트의 `Settings → Pages`에서 `Deploy from a branch`, `main`과 게임 파일 위치를 선택합니다.

PWA 설치와 오프라인 기능에는 HTTPS 주소가 필요합니다. ZIP 안의 `index.html`을 아이폰에서 직접 여는 방식으로는 설치형 기능이 작동하지 않습니다.

## 3. 아이폰 홈 화면에 설치하기

1. 아이폰 Safari에서 GitHub Pages 주소를 엽니다.
2. 공유 버튼을 누릅니다.
3. `홈 화면에 추가`를 선택합니다.
4. 이름이 `라면포차`인지 확인하고 `추가`를 누릅니다.
5. 홈 화면의 보름달 간판 아이콘으로 실행합니다.

처음 한 번은 인터넷에 연결한 상태로 로딩이 끝날 때까지 기다려 주세요. 핵심 이미지와 오프라인 파일을 저장한 뒤에는 연결이 없어도 다시 실행할 수 있습니다.

## 4. V0.22 업데이트가 보이지 않을 때

아이폰에서 홈 화면 앱을 완전히 종료한 뒤 다시 실행합니다. 그래도 이전 화면이면 Safari에서 Pages 주소를 한 번 열고 새로고침한 다음 홈 화면 앱을 다시 실행해 주세요. 필요하면 기존 홈 화면 아이콘을 지운 뒤 다시 추가합니다.

## 가로 방향에 관하여

V0.22는 설치형 앱에서 가로 방향을 우선 요청합니다. iOS가 방향 잠금을 허용하지 않아 세로 크기로 시작해도 게임 전체를 회전해 화면을 채우므로 휴대폰을 가로로 잡으면 처음부터 전체 게임이 보입니다.

아이폰 운영체제의 화면 방향 자체는 PWA가 모든 상황에서 강제로 바꿀 수 없습니다. 사용자의 회전 잠금 설정과 iOS 정책까지 무시하는 완전한 가로 고정이 필요하면 Capacitor/Xcode 네이티브 앱 단계에서 가로 방향만 허용해야 합니다.
