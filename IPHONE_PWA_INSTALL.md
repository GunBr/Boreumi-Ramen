# 아이폰 PWA 배포·설치 안내

## 1. GitHub에 올리기

V0.21 ZIP을 압축 해제한 뒤 `Boreumi-Ramen_V0.21` 폴더 안의 파일을 GitHub 저장소에 넣습니다. `index.html`, `app.webmanifest`, `service-worker.js`, `assets` 폴더가 서로 같은 배포 위치에 있어야 합니다.

GitHub Desktop에서 변경 내용을 커밋하고 `Push origin`을 눌러 GitHub에 올립니다.

## 2. GitHub Pages 켜기

1. GitHub 웹사이트에서 저장소를 엽니다.
2. `Settings → Pages`로 이동합니다.
3. `Deploy from a branch`를 선택합니다.
4. `main` 브랜치와 게임 파일이 들어 있는 위치를 선택합니다.
5. 저장 후 표시되는 `https://...github.io/...` 주소가 열릴 때까지 기다립니다.

PWA 설치와 오프라인 기능에는 HTTPS 주소가 필요합니다. ZIP 안의 `index.html`을 아이폰에서 직접 여는 방식으로는 설치형 기능이 작동하지 않습니다.

## 3. 아이폰 홈 화면에 설치하기

1. 아이폰 Safari에서 GitHub Pages 주소를 엽니다.
2. 공유 버튼을 누릅니다.
3. 메뉴에서 `홈 화면에 추가`를 선택합니다.
4. 이름이 `라면포차`인지 확인하고 `추가`를 누릅니다.
5. 홈 화면의 보름달 간판 아이콘을 눌러 실행합니다.

처음 한 번은 인터넷에 연결한 상태로 게임을 열어 주세요. 게임 이미지 저장이 끝난 뒤에는 연결이 없어도 다시 실행할 수 있습니다.

## 4. 업데이트 확인

새 버전을 GitHub에 올린 뒤 아이폰에서 앱을 완전히 종료하고 다시 실행합니다. 서비스 워커가 새 파일을 받으면 다음 실행부터 새 빌드가 사용됩니다. 이전 화면이 계속 보이면 홈 화면 앱을 다시 실행하거나 Safari에서 해당 주소를 한 번 열어 주세요.

## 가로 방향에 관하여

홈 화면 앱은 매니페스트에서 가로 방향으로 설정되어 있고, 지원되는 경우 실행 중 가로 잠금도 요청합니다. 단, PWA는 iOS의 최종 방향 정책과 사용자 설정을 따라야 합니다. 모든 아이폰에서 예외 없이 운영체제 수준으로 고정하려면 이후 Capacitor/Xcode 네이티브 앱 단계에서 가로 방향만 허용해야 합니다.
