# 보름이의 라면포차 V0.26.5 테스트 보고서

검사일: 2026-08-14

## 자동 검사

- PC 웹: 194/194 통과
- 모바일 가로 화면: 194/194 통과
- JavaScript 문법 검사: `script.js`, `service-worker.js`, `boot-v024.js`, `pwa-v024.js` 통과

## 이번 요청 전용 검사

- 손님 테이블이 무대 양끝에 맞고 아래선이 무대 기준 `y=632`에 맞는지 검사 통과
- 손님 줄, 주문 말풍선, 뽀미와 테이블의 하향 배치 검사 통과
- 새 수납형 테이블 WebP 로드와 오프라인 캐시 포함 검사 통과
- DAY 1 정산의 좌우 칸, 요약 카드, 상점 제목, 재료 보충, 강화 카드 경계 검사 통과
- 다음 날 시작 경로의 오뎅 빈 상태 초기화 검사 통과

## 실제 화면 확인

- PC 화면에서 테이블이 좌우 끝까지 이어지고 배경 선반선과 같은 높이에서 끝남
- 손님 상체가 상판 뒤에 자연스럽게 앉아 있고 하체가 수납형 테이블에 가려짐
- 뽀미가 오른쪽 상판에 걸치며 하단이 테이블 앞면 뒤에 자연스럽게 가려짐
- 소스통·꼬치·젓가락·수저·그릇·술병이 테이블에 붙인 아이콘이 아니라 수납칸 안의 소품으로 보임
- 첫날 영업일지에서 좌측 요약과 우측 강화 상점이 서로 침범하지 않음

## 캐시 확인

- 서비스 워커 캐시: `boreumi-ramen-v0265-0814b`
- 깨끗한 새 포트에서 PC와 모바일 가로 화면을 각각 다시 불러와 검사함

