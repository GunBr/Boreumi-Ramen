# 보름이의 라면포차 V0.26.4 작업 인계

## 현재 기준

- 버전: V0.26.4
- 일반 플레이: `index.html`
- 개발 도구: `index.html?dev`
- 자동 검사: `index.html?qa&silent`
- 일반 플레이와 개발 도구는 같은 게임 코드와 화면을 사용하고 개발 도구만 추가로 표시됩니다.

## 이번 버전의 확정 구성

- 손님과 포장 주문은 음식이 모두 서빙될 때까지 시간제한 없이 기다립니다.
- 완성 음식은 타지 않으며 계속 보온됩니다.
- 같은 날 같은 손님의 대화는 한 번만 표시합니다.
- 음식 주문은 현재 해금 메뉴 전체를 한 번씩 고르게 섞은 뒤 다음 묶음을 다시 섞습니다.
- 영업 중 발생한 매출은 즉시 보유 금액에 들어가며 정산에서 중복 지급하지 않습니다.
- 오뎅바 마지막 꼬치 서빙 뒤 오뎅 재료 1개를 써서 꼬치 3개를 자동 보충합니다.
- 손님 하체는 `assets/art-v0264/guest-counter-front-v1.webp` 통합 원목 앞판으로 가립니다.
- 포차 LV.2~LV.5를 새로 만들 때에도 손님 하체가 비어 보이는 얇은 선반 표현을 쓰지 않고, 손님과 배경 사이에 같은 화풍의 충분히 높은 통합 앞판을 둡니다.
- 재료 상점·메뉴·일지·가이드·음소거는 HUD 우측 아래 세로 한 줄 배치입니다.
- 정산 화면은 포차 LV.1~LV.5에서 동일하게 겹침 없는 고정 그리드를 사용합니다.

## 주요 변경 파일

- `index.html`: V0.26.4 버전, 손님석 앞판 요소, 정산 문구, 버튼 표기
- `script.js`: 메뉴 셔플, 대화 1회, 오뎅 자동 보충, 실시간 매출, 포장 무제한, 자동 검사
- `patch-v0264.css`: 손님석, 조리도구 정렬, 우측 버튼, 전 레벨 정산 레이아웃
- `service-worker.js`: V0.26.4 캐시와 새 아트 포함
- `boot-v024.js`, `pwa-v024.js`: V0.26.4 시작 처리
- `assets/art-v0264/`: 새 손님석 원본·투명 PNG·최적화 WebP

## 다음 작업 전 확인

- 변경은 항상 새 버전 폴더를 복사해 진행합니다.
- 빠른 기능 검사는 `?dev`, 전체 회귀 검사는 `?qa&silent`를 사용합니다.
- 서비스 워커 때문에 이전 파일이 보이면 캐시 버전과 `index.html`의 파일 쿼리 번호를 함께 올립니다.
- GitHub 저장소에는 100MB를 넘는 버전 ZIP을 커밋하지 말고, 압축을 푼 프로젝트 파일만 올립니다.

