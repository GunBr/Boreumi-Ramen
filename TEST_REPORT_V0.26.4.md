# 보름이의 라면포차 V0.26.4 테스트 보고서

검사일: 2026-08-13

## 자동 검사

- PC 웹: 190/190 통과
- 모바일 가로 화면: 190/190 통과
- JavaScript 문법 검사: `script.js`, `service-worker.js`, `boot-v024.js`, `pwa-v024.js` 통과

## 이번 요청 전용 검사

- 오뎅 꼬치 3→2→1 표시와 마지막 서빙 뒤 재료 1개 차감·3개 자동 보충 통과
- 같은 날 같은 손님 대화 1회 제한 통과
- 현재 해금 음식 전체가 중복 없이 한 번씩 나오는 메뉴 셔플 통과
- 손님석 앞판 리소스 로드, 손님 하체 가림, 보름이 표시 순서 통과
- 영업 중 매출의 보유 금액 즉시 반영과 정산 중복 지급 방지 통과
- 포장 주문 시간제한 제거와 무제한 대기 상태 통과
- 포차 LV.2 정산 화면 경계·열·하단 버튼 겹침 검사 통과

## 실제 화면 확인

- PC 영업 화면에서 손님 상체와 새 원목 앞판이 자연스럽게 연결됨
- 빈 의자와 손님 모두 같은 앞판 뒤에 위치하며 하체가 비어 보이지 않음
- 냄비·그릴·오뎅바가 같은 조리대 위에서 시각적으로 하단 정렬됨
- 우측 상단에 재료 상점·메뉴·일지·가이드·음소거 버튼이 세로로 표시됨
- 포차 LV.2 정산에서 안내 배너가 사라진 뒤 제목·결과·닫기·상점·강화 카드·하단 버튼 겹침 없음

## 캐시 확인

- 서비스 워커 캐시: `boreumi-ramen-v0264-0814c`
- 새 포트의 깨끗한 캐시 환경에서 PC와 모바일 가로 화면을 각각 다시 불러와 검사함

