# 보름이의 라면포차 V0.26.1 패치노트

## 조리와 음식 표현

- 면만 넣은 기본 라면에서 대파가 보이지 않도록 기본 조리·완성 일러스트를 교체했습니다.
- 대파를 넣으면 조리 중과 완성 후 모두 대파가 확실히 보이므로 기본 라면과 구분됩니다.

## 모바일 화면

- 아이폰 세로 상태에서 강제 가로 화면으로 실행할 때 시간·배터리 표시 영역을 피하도록 안전 여백 방향을 보정했습니다.
- 안전 여백을 제외한 실제 사용 가능 공간 안에서 게임 화면이 중앙 정렬되도록 크기 계산을 수정했습니다.
- 모바일에서 주문표를 덮던 메뉴·일지·도움말·소리·보유 금액 UI를 손님 좌석 바깥 오른쪽 공간에 정리했습니다.

## 보름이 이동

- 고정 좌표를 쓰지 않고 현재 보이는 냄비·그릴·오뎅바와 손님 좌석의 실제 중심을 계산하도록 변경했습니다.
- 포장마차 레벨, 조리도구 수, 좌석 수가 달라져도 조리·서빙 순간이동 위치가 따라갑니다.

## 영업과 정산

- 모든 DAY의 영업 시간을 5분으로 통일했습니다.
- 시간이 0이 되면 신규 주문만 마감하고, 남은 손님과 포장 주문을 모두 처리한 뒤 정산으로 넘어갑니다.
- 영업 현황판 오른쪽 위에 X 버튼을 추가했습니다.
- 현황판을 닫아도 상단 HUD의 `영업 시작` 버튼으로 다음 DAY 영업을 시작할 수 있습니다.

## 안정성

- 새 버전 적용 뒤 예전 오프라인 캐시의 파일이 섞이지 않도록 현재 버전 캐시만 조회하도록 수정했습니다.
- 실제 브라우저에서 모바일 배치, 주문 UI 겹침, 조리·서빙, 정산 닫기와 다음 DAY 시작을 포함한 자동 검사 181개를 통과했습니다.

