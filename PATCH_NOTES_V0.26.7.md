# 보름이의 라면포차 V0.26.7 패치 노트

- 손님 테이블을 y=454로 6px 더 내려 배경과의 남은 틈 제거
- 뽀미 그림의 실제 발 위치가 테이블 윗선에 닿도록 y=294로 재배치
- 손님 캐릭터의 `translateX(-50%)` 이동값까지 대화 좌표에 반영
- 말풍선이 화면 가장자리에서 이동해도 꼭지점만은 해당 손님 중심을 정확히 가리키도록 독립 좌표 적용
- 포장 주문판·주문 칸·라면 및 음식 그림·포장 그림 확대
- 10석 포차에서는 좌석과 겹치지 않는 전용 폭을 사용하면서도 기존보다 큰 음식 그림 유지
- 간판과 HUD를 왼쪽부터 이어 배치하고, 우측 도구 버튼 첫 줄을 HUD 윗선에 정렬
- 일시정지 화면을 실시간 영업일지로 교체하고 `계속 하기` 버튼 추가
- 새 버전 전용 CSS·실행 파일·서비스 워커 캐시 적용

## 버전

- 이전: V0.26.6
- 현재: V0.26.7
